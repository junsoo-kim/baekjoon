#include <stdio.h>

int main(void) 
{
	float a, b, c;
	float result;
	
	scanf("%f %f %f", &a, &b, &c);
	
	if(b >= c)
	{
		result = -1;
	}
	else
	{
		printf("%f\n", a / (c - b));
		result = a / (c - b) + 1;
	}
	
	printf("%d\n", (int)result);
	
	return 0;
}

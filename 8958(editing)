#include <stdio.h>

int main(void) 
{
	int n;
	int j = 0;
	char answer[80] = { 0 };
	
	scanf("%d", &n);
	
	for(int i = 0; i < n; i++)
	{
		while(1)
		{
			scanf("%c", &answer[i]);
			if(answer[i] == )
		}
	}
	return 0;
}
